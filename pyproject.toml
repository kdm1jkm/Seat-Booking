[project]
name = "seat-booking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.52",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
    "ty>=0.0.55",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["COM812", "D", "S311", "S605", "S607", "T201"]
